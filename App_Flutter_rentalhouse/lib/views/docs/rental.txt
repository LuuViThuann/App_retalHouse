1. FutureBuilder
👉 Mục đích:
FutureBuilder dùng để kết nối UI với dữ liệu bất đồng bộ (như khi gọi API, đọc file, truy vấn cơ sở dữ liệu...). Nó giúp hiển thị loading, error, hoặc dữ liệu thực tế tùy theo trạng thái của Future.

👉 Cách hoạt động:
Nó theo dõi trạng thái của một Future và render lại UI mỗi khi trạng thái thay đổi (đang chờ, hoàn tất, lỗi).

2. ListView.builder
👉 Mục đích:
ListView.builder dùng để xây dựng danh sách cuộn được và tối ưu hóa hiệu năng bằng cách tạo widget theo nhu cầu (on-demand). Nó không tạo tất cả widget ngay từ đầu, chỉ tạo khi cần hiển thị.

👉 Cách hoạt động:
Bạn cung cấp số lượng item và cách hiển thị từng item bằng itemBuilder.

3. ListView.separated
👉 Mục đích:
ListView.separated cũng tạo danh sách cuộn như ListView.builder, nhưng cho phép chèn widget ngăn cách (divider) giữa các item.

👉 Cách hoạt động:
Bạn cung cấp itemBuilder để tạo từng item.

Bạn cung cấp separatorBuilder để tạo widget phân cách giữa các item.